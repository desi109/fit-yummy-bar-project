import type { Stats, Configuration } from 'webpack';
import { Observable } from 'rxjs';
export declare function runWebpack(config: Configuration, webpack: typeof import('webpack')): Observable<Stats>;
export declare function runWebpackDevServer(config: Configuration, webpack: typeof import('webpack'), WebpackDevServer: typeof import('webpack-dev-server')): Observable<{
    stats: Stats;
    baseUrl: string;
}>;
export interface EmittedFile {
    id?: string;
    name?: string;
    file: string;
    extension: string;
    initial: boolean;
    asset?: boolean;
}
export declare function getEmittedFiles(stats: Stats): EmittedFile[];
