import { Rule } from '@angular-devkit/schematics';
import { Schema } from '../schema';
/**
 * Add ngrx feature exports to the public barrel in the feature library
 */
export declare function addExportsToBarrel(options: Schema): Rule;
