/**
 * Adapted from the original ngPackagr source.
 *
 * This initialization however does not print a warning when Ivy is disabled,
 * since the incremental build packages are not intended for distribution
 */
import { Transform } from 'ng-packagr/lib/graph/transform';
import { TransformProvider } from 'ng-packagr/lib/graph/transform.di';
import { ParsedConfiguration } from '@angular/compiler-cli';
export declare const initTsConfigTransformFactory: (defaultTsConfig: ParsedConfiguration) => Transform;
export declare const NX_INIT_TS_CONFIG_TRANSFORM: TransformProvider;
